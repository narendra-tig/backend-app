/**
 * @packageDocumentation
 * @module api.functional.v1.tracking.shipments.events
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";
import type { tags } from "typia";

import type { TenantHeaders } from "../../../../../structures/TenantHeaders";
import type { TrackingEventResponse } from "../../../../../structures/TrackingEventResponse";

/**
 * @controller TrackingFacadeController.getEvents
 * @path GET /v1/tracking/shipments/:shipmentId/events
 * @accessor api.functional.v1.tracking.shipments.events.getEvents
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getEvents(
  connection: IConnection<getEvents.Headers>,
  shipmentId: string &
    tags.TagBase<{
      target: "string";
      kind: "format";
      value: "uuid";
      validate: '$importInternal("isFormatUuid")($input)';
      exclusive: ["format", "pattern"];
      schema: {
        format: "uuid";
      };
    }>,
): Promise<getEvents.Output> {
  return PlainFetcher.fetch(connection, {
    ...getEvents.METADATA,
    template: getEvents.METADATA.path,
    path: getEvents.path(shipmentId),
  });
}
export namespace getEvents {
  export type Headers = TenantHeaders;
  export type Output = TrackingEventResponse[];

  export const METADATA = {
    method: "GET",
    path: "/v1/tracking/shipments/:shipmentId/events",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (
    shipmentId: string &
      tags.TagBase<{
        target: "string";
        kind: "format";
        value: "uuid";
        validate: '$importInternal("isFormatUuid")($input)';
        exclusive: ["format", "pattern"];
        schema: {
          format: "uuid";
        };
      }>,
  ) =>
    `/v1/tracking/shipments/${encodeURIComponent(
      shipmentId?.toString() ?? "null",
    )}/events`;
}
