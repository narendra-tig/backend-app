/**
 * @packageDocumentation
 * @module api.functional.v1.shipments
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";
import type { tags } from "typia";

import type { ShipmentListQuery } from "../../../structures/ShipmentListQuery";
import type { ShipmentListResponse } from "../../../structures/ShipmentListResponse";
import type { ShipmentResponse } from "../../../structures/ShipmentResponse";
import type { TenantHeaders } from "../../../structures/TenantHeaders";

export * as by_reference from "./by_reference/index";

/**
 * @controller ShipmentsController.list
 * @path GET /v1/shipments
 * @accessor api.functional.v1.shipments.list
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function list(
  connection: IConnection<list.Headers>,
  query: list.Query,
): Promise<list.Output> {
  return PlainFetcher.fetch(connection, {
    ...list.METADATA,
    template: list.METADATA.path,
    path: list.path(query),
  });
}
export namespace list {
  export type Headers = TenantHeaders;
  export type Query = ShipmentListQuery;
  export type Output = ShipmentListResponse;

  export const METADATA = {
    method: "GET",
    path: "/v1/shipments",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/v1/shipments";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}

/**
 * @controller ShipmentsController.findById
 * @path GET /v1/shipments/:shipmentId
 * @accessor api.functional.v1.shipments.findById
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function findById(
  connection: IConnection<findById.Headers>,
  shipmentId: string &
    tags.TagBase<{
      target: "string";
      kind: "format";
      value: "uuid";
      validate: '$importInternal("isFormatUuid")($input)';
      exclusive: ["format", "pattern"];
      schema: {
        format: "uuid";
      };
    }>,
): Promise<findById.Output> {
  return PlainFetcher.fetch(connection, {
    ...findById.METADATA,
    template: findById.METADATA.path,
    path: findById.path(shipmentId),
  });
}
export namespace findById {
  export type Headers = TenantHeaders;
  export type Output = ShipmentResponse;

  export const METADATA = {
    method: "GET",
    path: "/v1/shipments/:shipmentId",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (
    shipmentId: string &
      tags.TagBase<{
        target: "string";
        kind: "format";
        value: "uuid";
        validate: '$importInternal("isFormatUuid")($input)';
        exclusive: ["format", "pattern"];
        schema: {
          format: "uuid";
        };
      }>,
  ) => `/v1/shipments/${encodeURIComponent(shipmentId?.toString() ?? "null")}`;
}
