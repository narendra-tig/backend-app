/**
 * @packageDocumentation
 * @module api.functional.v1.tenancy.accounts.access
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";
import type { tags } from "typia";

import type { AccountAccessResponse } from "../../../../../structures/AccountAccessResponse";
import type { TenantContextQuery } from "../../../../../structures/TenantContextQuery";
import type { TenantHeaders } from "../../../../../structures/TenantHeaders";

/**
 * @controller TenancyController.verifyAccountAccess
 * @path GET /v1/tenancy/accounts/:accountId/access
 * @accessor api.functional.v1.tenancy.accounts.access.verifyAccountAccess
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function verifyAccountAccess(
  connection: IConnection<verifyAccountAccess.Headers>,
  accountId: string &
    tags.TagBase<{
      target: "string";
      kind: "format";
      value: "uuid";
      validate: '$importInternal("isFormatUuid")($input)';
      exclusive: ["format", "pattern"];
      schema: {
        format: "uuid";
      };
    }>,
  query: verifyAccountAccess.Query,
): Promise<verifyAccountAccess.Output> {
  return PlainFetcher.fetch(connection, {
    ...verifyAccountAccess.METADATA,
    template: verifyAccountAccess.METADATA.path,
    path: verifyAccountAccess.path(accountId, query),
  });
}
export namespace verifyAccountAccess {
  export type Headers = TenantHeaders;
  export type Query = TenantContextQuery;
  export type Output = AccountAccessResponse;

  export const METADATA = {
    method: "GET",
    path: "/v1/tenancy/accounts/:accountId/access",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (
    accountId: string &
      tags.TagBase<{
        target: "string";
        kind: "format";
        value: "uuid";
        validate: '$importInternal("isFormatUuid")($input)';
        exclusive: ["format", "pattern"];
        schema: {
          format: "uuid";
        };
      }>,
    query: Query,
  ) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = `/v1/tenancy/accounts/${encodeURIComponent(
      accountId?.toString() ?? "null",
    )}/access`;
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
