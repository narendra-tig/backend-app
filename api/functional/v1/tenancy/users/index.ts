/**
 * @packageDocumentation
 * @module api.functional.v1.tenancy.users
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";

import type { TenantHeaders } from "../../../../structures/TenantHeaders";
import type { TenantUsersQuery } from "../../../../structures/TenantUsersQuery";
import type { TenantUsersResponse } from "../../../../structures/TenantUsersResponse";

/**
 * @controller TenancyController.listUsers
 * @path GET /v1/tenancy/users
 * @accessor api.functional.v1.tenancy.users.listUsers
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function listUsers(
  connection: IConnection<listUsers.Headers>,
  query: listUsers.Query,
): Promise<listUsers.Output> {
  return PlainFetcher.fetch(connection, {
    ...listUsers.METADATA,
    template: listUsers.METADATA.path,
    path: listUsers.path(query),
  });
}
export namespace listUsers {
  export type Headers = TenantHeaders;
  export type Query = TenantUsersQuery;
  export type Output = TenantUsersResponse;

  export const METADATA = {
    method: "GET",
    path: "/v1/tenancy/users",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/v1/tenancy/users";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
