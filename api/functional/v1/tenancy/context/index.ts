/**
 * @packageDocumentation
 * @module api.functional.v1.tenancy.context
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";

import type { TenantContextQuery } from "../../../../structures/TenantContextQuery";
import type { TenantContextResponse } from "../../../../structures/TenantContextResponse";
import type { TenantHeaders } from "../../../../structures/TenantHeaders";

/**
 * @controller TenancyController.getContext
 * @path GET /v1/tenancy/context
 * @accessor api.functional.v1.tenancy.context.getContext
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getContext(
  connection: IConnection<getContext.Headers>,
  query: getContext.Query,
): Promise<getContext.Output> {
  return PlainFetcher.fetch(connection, {
    ...getContext.METADATA,
    template: getContext.METADATA.path,
    path: getContext.path(query),
  });
}
export namespace getContext {
  export type Headers = TenantHeaders;
  export type Query = TenantContextQuery;
  export type Output = TenantContextResponse;

  export const METADATA = {
    method: "GET",
    path: "/v1/tenancy/context",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/v1/tenancy/context";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
