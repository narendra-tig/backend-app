/**
 * @packageDocumentation
 * @module api.functional.v1.locations
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";

import type { LocationSearchQuery } from "../../../structures/LocationSearchQuery";
import type { SuburbLocation } from "../../../structures/SuburbLocation";
import type { TenantHeaders } from "../../../structures/TenantHeaders";

/**
 * @controller LocationsController.search
 * @path GET /v1/locations/search
 * @accessor api.functional.v1.locations.search
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function search(
  connection: IConnection<search.Headers>,
  query: search.Query,
): Promise<search.Output> {
  return PlainFetcher.fetch(connection, {
    ...search.METADATA,
    template: search.METADATA.path,
    path: search.path(query),
  });
}
export namespace search {
  export type Headers = TenantHeaders;
  export type Query = LocationSearchQuery;
  export type Output = SuburbLocation[];

  export const METADATA = {
    method: "GET",
    path: "/v1/locations/search",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/v1/locations/search";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
