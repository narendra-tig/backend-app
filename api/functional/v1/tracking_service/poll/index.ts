/**
 * @packageDocumentation
 * @module api.functional.v1.tracking_service.poll
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher";

import type { InternalHeaders } from "../../../../structures/InternalHeaders";
import type { PollTrackingQuery } from "../../../../structures/PollTrackingQuery";
import type { TrackingShipmentsResponse } from "../../../../structures/TrackingShipmentsResponse";

/**
 * @controller TrackingController.getShipmentsForTracking
 * @path GET /v1/tracking-service/poll
 * @accessor api.functional.v1.tracking_service.poll.getShipmentsForTracking
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function getShipmentsForTracking(
  connection: IConnection<getShipmentsForTracking.Headers>,
  query: getShipmentsForTracking.Query,
): Promise<getShipmentsForTracking.Output> {
  return PlainFetcher.fetch(connection, {
    ...getShipmentsForTracking.METADATA,
    template: getShipmentsForTracking.METADATA.path,
    path: getShipmentsForTracking.path(query),
  });
}
export namespace getShipmentsForTracking {
  export type Headers = InternalHeaders;
  export type Query = PollTrackingQuery;
  export type Output = TrackingShipmentsResponse;

  export const METADATA = {
    method: "GET",
    path: "/v1/tracking-service/poll",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/v1/tracking-service/poll";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
